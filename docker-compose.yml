services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: bulkmarket
      POSTGRES_USER: bulkmarket
      POSTGRES_PASSWORD: bulkmarket
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data:
